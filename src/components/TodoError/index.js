function TodoError({error}) {
  const styleError = {
    textAlign: "center",
    color: "white",
    paddingTop: "20px",
  };
  return <p style={styleError}>hubo un error</p>;
}

export { TodoError };